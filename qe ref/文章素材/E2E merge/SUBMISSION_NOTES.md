# Submission notes

- `main.pdf` contains seven pages of technical content. The conclusion ends on page 7 and references begin on page 8.
- `supplement.pdf` is a separate 12-page technical supplement.
- `full.pdf` is an internal combined version and should not be uploaded as the limited-page main paper.
- The official AAAI-26 `aaai2026.sty` and `aaai2026.bst` files are included without modification.
- No additional experiment was introduced. See `DATA_PROVENANCE.md` for every reported numerical value and arithmetic summary.
- `ReproducibilityChecklist.tex` is answered from the paper (31 items). It compiles standalone with the AAAI-2027 kit style (`aaai2027.sty`, which requires the `newtx` fonts). Please re-verify each response before upload.
- The anonymized code and data supplement is prepared separately as `code_supplement.zip` (E2E-Merge method, configs, analysis, and reproduction scripts). It contains no author, institution, or machine identifiers.
- `supplement.tex` is self-contained: it no longer uses cross-document (`xr`) references and compiles independently of `main.tex`.
- All manuscript sources are reflowed to one sentence per source line for readability; this does not change the compiled output.
